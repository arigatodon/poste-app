<h1 class="container"> Publicaciones guardadas</h1>


<div class="table table-condensed"></div>   	
	<table class="table table-condensed">
		<tr>
		<th class="active">Titulo</th>
		  <th class="active">Telefono</th>
		  <th class="active">Quitar de marcadores</tr>
		  
		<% current_user.favorites.each do |post| %>  
		  <td class="active"><%= post.title %></td>
		  <td class="active"><%= post.phone %></td>
		  <td class="danger"><%= link_to "borrar", favorite_post_path(post, type: "unfavorite"), method: :put %></td>	
		</tr>
		 <%end  %>
	</table>
</div>
